apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: karakeep-cert
spec:
  secretName: karakeep-cert-secret
  issuerRef:
    name: contour-selfsigned-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - karakeep.kubernetes.home
