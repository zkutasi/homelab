apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: it-tools-cert
spec:
  secretName: it-tools-cert-secret
  issuerRef:
    name: contour-selfsigned-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - ittools.kubernetes.home
