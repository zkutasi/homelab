apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: databasus-cert
spec:
  secretName: databasus-cert-secret
  issuerRef:
    name: contour-selfsigned-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - databasus.kubernetes.home
