apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: portabase-cert
spec:
  secretName: portabase-cert-secret
  issuerRef:
    name: contour-selfsigned-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - portabase.kubernetes.home
