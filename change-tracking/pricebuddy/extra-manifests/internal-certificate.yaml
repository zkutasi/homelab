apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pricebuddy-cert
spec:
  secretName: pricebuddy-cert-secret
  issuerRef:
    name: contour-selfsigned-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - pricebuddy.kubernetes.home
