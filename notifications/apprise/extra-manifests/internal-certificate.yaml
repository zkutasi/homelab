apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: apprise-cert
spec:
  secretName: apprise-cert-secret
  issuerRef:
    name: contour-selfsigned-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - apprise.kubernetes.home
