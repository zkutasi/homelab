apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kubewall-cert
spec:
  secretName: kubewall-cert-secret
  issuerRef:
    name: contour-selfsigned-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - kubewall.kubernetes.home
