apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  labels:
    k8s-app: bentopdf
  name: bentopdf-httpproxy
spec:
  routes:
    - conditions:
        - prefix: /
      services:
        - name: bentopdf
          port: 8080
  virtualhost:
    corsPolicy:
      allowCredentials: true
      allowOrigin:
        - "*" # allows any origin
      allowMethods:
        - GET
        - POST
        - OPTIONS
      allowHeaders:
        - authorization
        - cache-control
      exposeHeaders:
        - Content-Length
        - Content-Range
      maxAge: "10m" # preflight requests can be cached for 10 minutes.
    fqdn: bentopdf.kubernetes.home
    tls:
      secretName: bentopdf-cert-secret
