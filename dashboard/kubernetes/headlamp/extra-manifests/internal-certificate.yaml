apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: headlamp-cert
spec:
  secretName: headlamp-cert-secret
  issuerRef:
    name: contour-selfsigned-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - headlamp.kubernetes.home
