apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: helm-dashboard-cert
spec:
  secretName: helm-dashboard-cert-secret
  issuerRef:
    name: contour-selfsigned-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - helm-dashboard.kubernetes.home
