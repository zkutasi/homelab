apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  labels:
    k8s-app: cyberchef
  name: cyberchef-httpproxy
spec:
  routes:
    - conditions:
        - prefix: /
      services:
        - name: cyberchef
          port: 10219
  virtualhost:
    corsPolicy:
      allowCredentials: true
      allowOrigin:
        - "*"  # allows any origin
      allowMethods:
        - GET
        - POST
        - OPTIONS
      allowHeaders:
        - authorization
        - cache-control
      exposeHeaders:
        - Content-Length
        - Content-Range
      maxAge: "10m"  # preflight requests can be cached for 10 minutes.
    fqdn: cyberchef.kubernetes.home
    tls:
      secretName: cyberchef-cert-secret
