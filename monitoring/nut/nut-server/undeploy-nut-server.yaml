- name: Delete Nut-server as service
  hosts: "{{ groups['nutserver'] | default([]) | intersect(groups['linux-selfmanaged'] | default([])) }}"
  roles:
    - package-remove
  vars:
    packages:
      - libusb-1.0-0-dev
      - nut
