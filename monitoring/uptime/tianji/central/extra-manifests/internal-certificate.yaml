apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tianji-cert
spec:
  secretName: tianji-cert-secret
  issuerRef:
    name: contour-selfsigned-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - tianji.kubernetes.home
