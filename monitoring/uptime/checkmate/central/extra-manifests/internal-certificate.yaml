apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: checkmate-cert
spec:
  secretName: checkmate-cert-secret
  issuerRef:
    name: contour-selfsigned-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - checkmate.kubernetes.home
