apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: whisker-cert
spec:
  secretName: whisker-cert-secret
  issuerRef:
    name: contour-selfsigned-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - whisker.kubernetes.home
