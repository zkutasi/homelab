---
- name: Copy media aggregator script
  copy:
    src: media.sh
    dest: "{{ user_home }}/media.sh"
    mode: 0755
- name: Copy subtitle aggregator script
  copy:
    src: subtitles.sh
    dest: "{{ user_home }}/subtitles.sh"
    mode: 0755
- name: Copy hasher script
  copy:
    src: hasher.sh
    dest: "{{ user_home }}/hasher.sh"
    mode: 0755
