apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: loki-cert
spec:
  secretName: loki-cert-secret
  issuerRef:
    name: contour-selfsigned-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - loki.kubernetes.home
