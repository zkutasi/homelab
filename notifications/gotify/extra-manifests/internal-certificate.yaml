apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gotify-cert
spec:
  secretName: gotify-cert-secret
  issuerRef:
    name: contour-selfsigned-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - gotify.kubernetes.home
