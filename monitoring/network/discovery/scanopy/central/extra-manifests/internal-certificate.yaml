apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: scanopy-cert
spec:
  secretName: scanopy-cert-secret
  issuerRef:
    name: contour-selfsigned-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - scanopy.kubernetes.home
