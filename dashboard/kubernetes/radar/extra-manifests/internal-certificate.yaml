apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: radar-cert
spec:
  secretName: radar-cert-secret
  issuerRef:
    name: contour-selfsigned-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - radar.kubernetes.home
