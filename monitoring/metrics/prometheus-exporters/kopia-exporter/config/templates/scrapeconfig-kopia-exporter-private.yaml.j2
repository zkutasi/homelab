apiVersion: monitoring.coreos.com/v1alpha1
kind: ScrapeConfig
metadata:
  name: kopia-exporter
  labels:
    release: kube-prometheus-stack
spec:
  jobName: kopia-exporter
  staticConfigs:
{% for host in groups['backupserver'] | default([]) %}
    - targets: ["{{ hostvars[host].ansible_host }}:{{ port_kopia_exporter }}"]
      labels:
        host: "{{ hostvars[host].id }}"
{% endfor %}
