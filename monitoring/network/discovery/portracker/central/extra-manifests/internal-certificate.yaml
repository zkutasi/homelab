apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: portracker-cert
spec:
  secretName: portracker-cert-secret
  issuerRef:
    name: contour-selfsigned-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - portracker.kubernetes.home
