apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  labels:
    k8s-app: karakeep
  name: karakeep-httpproxy
spec:
  routes:
    - conditions:
        - prefix: /
      services:
        - name: karakeep
          port: 3000
  virtualhost:
    corsPolicy:
      allowCredentials: true
      allowOrigin:
        - "*"  # allows any origin
      allowMethods:
        - GET
        - POST
        - OPTIONS
      allowHeaders:
        - authorization
        - cache-control
      exposeHeaders:
        - Content-Length
        - Content-Range
      maxAge: "10m"  # preflight requests can be cached for 10 minutes.
    fqdn: karakeep.kubernetes.home
    tls:
      secretName: karakeep-cert-secret
