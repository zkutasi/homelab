- name: Restart the patchmon-agent service
  hosts:
    - proxmox
    - standalone
  become: true
  tasks:
    - name: Restart patchmon-agent service
      ansible.builtin.systemd:
        name: patchmon-agent
        state: restarted
        enabled: true
