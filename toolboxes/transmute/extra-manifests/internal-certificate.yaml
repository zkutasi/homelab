apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: transmute-cert
spec:
  secretName: transmute-cert-secret
  issuerRef:
    name: contour-selfsigned-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - transmute.kubernetes.home
