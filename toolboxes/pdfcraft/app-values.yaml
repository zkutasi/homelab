image:
  repository: ghcr.io/pdfcrafttool/pdfcraft
  tag: sha-93fe3ca # v2026.03.25
  pullPolicy: IfNotPresent
persistence:
  nginx-cache:
    enabled: true
    type: emptyDir
    mountPath: /var/cache/nginx
service:
  main:
    enabled: true
    ports:
      main:
        port: 80
        protocol: http
        targetPort: 80
TZ: Europe/Budapest
workload:
  main:
    enabled: true
    type: Deployment
    podSpec:
      containers:
        main:
          enabled: true
          probes:
            liveness:
              enabled: false
            readiness:
              enabled: false
            startup:
              enabled: false
          securityContext:
            runAsUser: 0
            runAsGroup: 0
