services:
  node_exporter:
    command:
      - '--path.rootfs=/host'
    container_name: node_exporter
    image: quay.io/prometheus/node-exporter:v{{ requested_version }}
    network_mode: host
    pid: host
    restart: unless-stopped
    volumes:
      - '/:/host:ro,rslave'
