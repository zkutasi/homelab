apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: omnisight-cert
spec:
  secretName: omnisight-cert-secret
  issuerRef:
    name: contour-selfsigned-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - omnisight.kubernetes.home
