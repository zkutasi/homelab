image:
  repository: ghcr.io/wasi-master/13ft
  tag: 0.3.4
  pullPolicy: IfNotPresent
service:
  main:
    enabled: true
    ports:
      main:
        port: 5000
        protocol: http
        targetPort: 5000
TZ: Europe/Budapest
workload:
  main:
    enabled: true
    type: Deployment
    podSpec:
      containers:
        main:
          enabled: true
          probes:
            liveness:
              enabled: false
            readiness:
              enabled: false
            startup:
              enabled: false
