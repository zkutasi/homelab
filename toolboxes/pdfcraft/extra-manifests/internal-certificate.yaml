apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pdfcraft-cert
spec:
  secretName: pdfcraft-cert-secret
  issuerRef:
    name: contour-selfsigned-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - pdfcraft.kubernetes.home
