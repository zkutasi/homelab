apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ceph-dashboard-cert
spec:
  secretName: ceph-dashboard-cert-secret
  issuerRef:
    name: contour-selfsigned-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - ceph.kubernetes.home
