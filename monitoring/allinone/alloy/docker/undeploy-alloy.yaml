- name: Delete alloy containers
  hosts: "{{ groups['docker'] | default([]) | difference(groups['external'] | default([])) }}"
  roles:
    - docker-compose-remove
  vars:
    docker_compose_projectname: alloy
