- name: Generate dynamic configuration for Docker Daemon
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Make sure extra-manifests directory exists
      ansible.builtin.file:
        path: extra-manifests
        state: directory
    - name: Generate configuration file from template
      ansible.builtin.template:
        src: config/templates/scrapeconfig-docker-daemon-private.yaml.j2
        dest: "extra-manifests/scrapeconfig-docker-daemon-private.yaml"
