apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pdfding-cert
spec:
  secretName: pdfding-cert-secret
  issuerRef:
    name: contour-selfsigned-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - pdfding.kubernetes.home
