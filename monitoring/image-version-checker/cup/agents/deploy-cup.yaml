- name: Deploy cup as docker
  hosts: docker
  roles:
    - docker-compose
  vars:
    docker_compose_projectname: cup
    requested_image_version:
      cup: "3.5.1"
    docker_compose_create_dirs:
      - name: config
    docker_compose_template_files_copy:
      - name: config/cup.json
