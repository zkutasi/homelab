resources:
  - all.yaml
patches:
  - target:
      group: apps
      version: v1
      kind: DaemonSet
      name: contour-envoy
    patch: |-
      - op: add
        path: /spec/template/spec/initContainers/0/args/-
        value: --dns-lookup-family=v4
