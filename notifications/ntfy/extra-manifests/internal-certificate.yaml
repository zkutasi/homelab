apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ntfy-cert
spec:
  secretName: ntfy-cert-secret
  issuerRef:
    name: contour-selfsigned-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - ntfy.kubernetes.home
