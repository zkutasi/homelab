apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  labels:
    k8s-app: librenms
  name: librenms-httpproxy
spec:
  routes:
    - conditions:
        - prefix: /
      services:
        - name: librenms
          port: 8000
  virtualhost:
    corsPolicy:
      allowCredentials: true
      allowOrigin:
        - "*" # allows any origin
      allowMethods:
        - GET
        - POST
        - OPTIONS
      allowHeaders:
        - authorization
        - cache-control
      exposeHeaders:
        - Content-Length
        - Content-Range
      maxAge: "10m" # preflight requests can be cached for 10 minutes.
    fqdn: librenms.kubernetes.home
    tls:
      secretName: librenms-cert-secret
