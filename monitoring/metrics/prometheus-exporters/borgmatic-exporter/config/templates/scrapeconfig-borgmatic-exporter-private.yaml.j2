apiVersion: monitoring.coreos.com/v1alpha1
kind: ScrapeConfig
metadata:
  name: borgmatic-exporter
  labels:
    release: kube-prometheus-stack
spec:
  jobName: borgmatic-exporter
  staticConfigs:
{% for host in groups['backupserver'] | default([]) %}
    - targets: ["{{ hostvars[host].ansible_host }}:{{ port_borgmatic_exporter }}"]
      labels:
        host: "{{ hostvars[host].id }}"
{% endfor %}
