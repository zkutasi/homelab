- name: Generate dynamic configuration for Caddy
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Make sure extra-manifests directory exists
      ansible.builtin.file:
        path: extra-manifests
        state: directory
    - name: Generate configuration file from template
      ansible.builtin.template:
        src: config/templates/scrapeconfig-caddy-private.yaml.j2
        dest: "extra-manifests/scrapeconfig-caddy-private.yaml"
