- name: Delete smartctl as a service
  hosts: "{{ groups['smart'] | difference(groups['docker']) }}"
  roles:
    - binary-remove
  vars:
    bin_filename: smartctl
    service_name: smartctl-exporter
