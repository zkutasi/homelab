[Unit]
Description={{ bin_filename }}
After=network.target
StartLimitIntervalSec=0

[Service]
Type=simple
Restart=always
RestartSec=1
User=root
ExecStart={{ bin_path }}/{{ bin_filename }}

[Install]
WantedBy=multi-user.target