- name: Deploy oras
  hosts: controlserver
  roles:
    - binary-install
  vars:
    requested_binary_version: "1.3.1"
    bin_filename: oras
    download_url: "https://github.com/oras-project/oras/releases/download/\
          v{{ requested_binary_version }}/{{ bin_filename }}_{{ requested_binary_version }}_linux_amd64.tar.gz"
    version_flag: "version"
    decompression_options:
      - "--wildcards"
      - "{{ bin_filename }}*"
