apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: adminer-cert
spec:
  secretName: adminer-cert-secret
  issuerRef:
    name: contour-selfsigned-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - adminer.kubernetes.home
