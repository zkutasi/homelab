apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: glances-cert
spec:
  secretName: glances-cert-secret
  issuerRef:
    name: contour-selfsigned-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - glances.kubernetes.home
