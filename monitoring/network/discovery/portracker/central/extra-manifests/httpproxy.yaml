apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  labels:
    k8s-app: portracker
  name: portracker-httpproxy
spec:
  routes:
    - conditions:
        - prefix: /
      services:
        - name: portracker
          port: 4999
  virtualhost:
    corsPolicy:
      allowCredentials: true
      allowOrigin:
        - "*"  # allows any origin
      allowMethods:
        - GET
        - POST
        - OPTIONS
      allowHeaders:
        - authorization
        - cache-control
      exposeHeaders:
        - Content-Length
        - Content-Range
      maxAge: "10m"  # preflight requests can be cached for 10 minutes.
    fqdn: portracker.kubernetes.home
    tls:
      secretName: portracker-cert-secret
