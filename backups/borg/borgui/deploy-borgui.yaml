- name: Deploy BorgUI as docker
  hosts: backupserver
  roles:
    - docker-compose
  vars:
    docker_compose_projectname: borgui
    requested_image_version:
      borgui: "ainullcode/borg-ui:2.2.5"
    docker_compose_create_dirs:
      - name: borg_cache
      - name: borg_data
