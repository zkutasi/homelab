apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: reubah-cert
spec:
  secretName: reubah-cert-secret
  issuerRef:
    name: contour-selfsigned-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - reubah.kubernetes.home
