apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vert-cert
spec:
  secretName: vert-cert-secret
  issuerRef:
    name: contour-selfsigned-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - vert.kubernetes.home
