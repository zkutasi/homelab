apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: snapotter-cert
spec:
  secretName: snapotter-cert-secret
  issuerRef:
    name: contour-selfsigned-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - snapotter.kubernetes.home
