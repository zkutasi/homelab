- name: Deploy komodo-periphery as docker
  hosts: docker
  roles:
    - docker-compose
  vars:
    docker_compose_projectname: komodo-periphery
    requested_image_version:
      komodo-periphery: "ghcr.io/moghtech/komodo-periphery:2.2.0"
    docker_compose_create_dirs:
      - name: config
