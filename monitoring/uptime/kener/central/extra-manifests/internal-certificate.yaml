apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kener-cert
spec:
  secretName: kener-cert-secret
  issuerRef:
    name: contour-selfsigned-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - kener.kubernetes.home
